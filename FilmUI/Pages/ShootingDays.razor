@page "/shootingdays"

<h3 class="mb-4">Shooting Days</h3>

@if (shootingDays == null)
{
    <div class="spinner-border text-primary" role="status">
        <span class="visually-hidden">Loading...</span>
    </div>
}
else if (shootingDays.Count == 0)
{
    <div class="alert alert-warning" role="alert">
        No shooting days found.
    </div>
}
else
{
    <ul class="list-group">
        @foreach (var shot in shootingDays)
        {
            <li class="list-group-item">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <div class="fw-bold">@shot.Number @shot.ShortDescription</div>
                        <small class="text-muted">Shooting Time: @shot.ShootingTime</small>
                    </div>
                </div>
            </li>
        }
    </ul>
}
