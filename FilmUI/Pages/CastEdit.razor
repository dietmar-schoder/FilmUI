@page "/cast/edit/{Id:int}"

<div class="d-flex align-items-center mb-3">
    <button class="btn btn-link p-0 me-2" @onclick="GoBack" title="Go back" style="font-size: 1.5rem;">
        <i class="bi bi-arrow-left"></i>
    </button>
    <h3 class="m-0">Edit Cast Member</h3>
</div>

@if (cast is null)
{
    <div class="spinner-border text-primary" role="status">
        <span class="visually-hidden">Loading...</span>
    </div>
}
else
{
    <div class="mb-3">
        <label class="form-label fw-bold">Role</label>
        <input class="form-control" @bind="cast.Role" />
    </div>
    <div class="mb-3">
        <label class="form-label fw-bold">Actor</label>
        <input class="form-control" @bind="cast.Actor" />
    </div>
    <div class="d-flex flex-column gap-2" style="width: 150px;">
        <button class="btn btn-outline-secondary" @onclick="GoBack">Cancel</button>
        <button class="btn btn-primary" @onclick="Save">Save</button>
    </div>
}
